# Anthropic API key for the LLM fallback layer (extraction of irregular fields).
# Get one at https://console.anthropic.com/. Without this key the system still
# runs end-to-end; only the LLM fallback layer is automatically disabled.
ANTHROPIC_API_KEY=sk-ant-replace-me
